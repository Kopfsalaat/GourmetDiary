import { connect } from 'react-redux';

function Footer(){
    return(
        <div>
            Footer
        </div>
    )
}

const mapStateToProp = state =>({

})

export default connect(mapStateToProp,{

}) (Footer)